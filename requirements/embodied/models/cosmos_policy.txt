# Extra pins for the Cosmos Policy (RoboCasa) RLinf integration.
#
# The heavy dependencies (torch, transformers, robosuite, mujoco, cosmos-predict2)
# come from the official cosmos-policy pyproject, installed via
# `uv pip install -e <cosmos-policy>` in install_cosmos_policy_model().
#
# Keep this file minimal to avoid re-solving cosmos-policy's own pinned stack.
# Add only RLinf-side runtime helpers that cosmos-policy does not already provide.
#
# TODO(verify): if `import cosmos_policy` pulls a transformers/robosuite version
# that breaks RLinf's RobocasaEnv, pin the reconciled versions here (see R1 in
# dreamzero_docs/robocasa_cosmos_rlinf_integration_plan_README.md).
imageio-ffmpeg
